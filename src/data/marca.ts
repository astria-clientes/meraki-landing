/* ==========================================================================
 *  LOGOS
 *  Subí los archivos a /public/logos/ y escribí acá la ruta (ej. "/logos/meraki.png").
 *  Mientras quede en null, la página muestra un recuadro "logo pendiente".
 *  Formatos recomendados: PNG con fondo transparente o SVG.
 * ========================================================================== */

export const marca = {
  /** Logo de MERAKI (peluquería / barbería). Se usa en el capítulo 01. */
  logoMeraki: null as string | null,

  /**
   * Logo de la marca de piedras y terapias (en diseño).
   * Se usa en los capítulos 02 y 03.
   */
  logoPiedras: null as string | null,

  /** Nombre de esa segunda marca, si lo tiene. null = no se muestra. */
  nombreMarcaPiedras: null as string | null,
};
